# Training-only dependencies for the VITS acoustic model (heavy, GPU-oriented).
# Install these in a SEPARATE environment from voicelk_ml/requirements.txt, which is the
# lightweight NLP-only environment for the Flask API service. Do not mix the two venvs.
#
# NEITHER `TTS` (Vits / AudioProcessor / TTSTokenizer) NOR `trainer` (Trainer / TrainerArgs)
# is installed via pip — both are vendored as actual source code directly in this project:
#   model_training/TTS/       (copied from TTS-dev/TTS-dev/TTS)
#   model_training/trainer/   (from the `trainer` PyPI sdist, which doesn't build on
#                              Python >=3.12 — see model_training/trainer/VERSION)
# train_*.py's `from TTS...` / `from trainer import ...` lines resolve to these local
# copies automatically (Python adds a script's own directory to sys.path). The packages
# below are the generic third-party libraries TTS/trainer themselves need at import time —
# ordinary PyPI dependencies, not project code, so they're still installed normally.

torch>=2.0.0
torchaudio>=2.0.0
pandas>=2.0.0
soundfile>=0.12.0
librosa>=0.10.0
tqdm>=4.65.0
coqpit>=0.0.16
psutil>=5.9.0
fsspec>=2023.6.0
tensorboard>=2.13.0
numpy>=1.24.3
cython>=0.29.30
scipy>=1.11.2
scikit-learn>=1.3.0
numba>=0.57.0
inflect>=5.6.0
anyascii>=0.3.0
pyyaml>=6.0
aiohttp>=3.8.1
packaging>=23.1
mutagen>=1.47.0
pysbd>=0.3.4
matplotlib>=3.7.0
